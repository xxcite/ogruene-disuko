# i18n Domain Package

This package contains the domain models for i18n (internationalization) persistence.

## Types

- `I18nLocale` - Represents a language locale with all translation entries
- `I18nEntry` - Represents a single translation entry (key-value pair)
